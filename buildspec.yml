version: 0.1

environment_variables:
  plaintext:
    JAVA_HOME: "/usr/lib/jvm/java-8-openjdk-amd64"

phases:
  install:
    commands:
#      - apt-get update -y
#      - apt-get install -y maven
  Pre_build:
    Commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
#      - mvn install
  post_build:
    commands:
      - aws cloudformation package --template /cp-update-template_tr.json --s3-bucket  update-poc-storage --output json > new_cp-update-template_tr.json
      - echo Build completed on `date`
artifacts:
  files:
#    - target/messageUtil-1.0.jar
    - new_cp-update-template_tr.json
  discard-paths: yes
